# ComplaintCare (ResolveNow) - Frontend

React frontend for the Online Complaint Registration and Management System.

## Setup

1. Install dependencies: `npm install`
2. Ensure backend is running on `http://localhost:8000`
3. Start dev server: `npm start`

Open [http://localhost:3000](http://localhost:3000). Use SignUp to create admin/agent/customer accounts.
